<RichItem>
  <div class="item-{ props.title }">
    <div id="title{ Math.random() }">
      <h2>{ props.title }</h2>
      <RichItemSubtitle id="{ Math.random() }">Subtitle - { Math.random() }</RichItemSubtitle>
      <p id="p-{ props.title }">{ props.content }</p>
    </div>
  </div>

  <script>
    import RichItemSubtitle from './RichItemSubtitle.riot'

    export default {
      components: {
        RichItemSubtitle
      }
    }
  </script>
</RichItem>
